---
import { ThemeSwitch } from "free-astro-components";
import Hamburguer from "./Hamburguer.astro";

const base = (import.meta.env.BASE_URL || "/").replace(/\/$/, "");
const homeUrl = base ? `${base}/` : "/";
const postsUrl = `${base}/posts`;
const docUrl = `${base}/posts/who-we-are`;
---

<header
  class="fixed inset-x-0 top-0 bg-neutral-100/70 text-neutral-700 py-3.5 border-b border-neutral-300/50 backdrop-blur-md z-20"
>
  <div class="container flex justify-between items-center gap-6">
    <a
      class="flex items-center gap-2.5 text-primary hover:text-secondary text-2xl font-bold leading-none transition duration-300 ease-in-out"
      href={homeUrl}
      aria-label="YouthX"
    >
      <img src={`${base}/logo.png`} alt="YouthX Logo" class="w-8 h-8 rounded-lg object-contain" />
      <span class="text-xl font-bold tracking-tight text-neutral-700">YouthX</span>
    </a>

    <nav>
      <ul class="flex items-center gap-6 md:gap-8">
        <li>
          <a
            class="text-sm hover:text-primary transition duration-300 ease-in-out"
            href={postsUrl}>Articles</a
          >
        </li>
        <li>
          <a
            class="text-sm hover:text-primary transition duration-300 ease-in-out"
            href={docUrl}>About & Docs</a
          >
        </li>
        <li class="hidden sm:block">
          <a
            class="text-sm hover:text-primary transition duration-300 ease-in-out flex items-center gap-1"
            href="https://www.instagram.com/youthx_research/"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span>Instagram</span>
          </a>
        </li>
        <li class="hidden sm:block">
          <a
            class="text-sm hover:text-primary transition duration-300 ease-in-out flex items-center gap-1"
            href="https://github.com/YouthX-Research"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span>GitHub</span>
          </a>
        </li>
        <li>
          <ThemeSwitch />
        </li>
        <li class="lg:hidden">
          <Hamburguer />
        </li>
      </ul>
    </nav>
  </div>
</header>

<script>
  import { DOMLoaded } from "../ts/utils";

  DOMLoaded(() => {
    const body = document.querySelector("body") as HTMLElement;
    const hamburguer = document.querySelector(
      "[data-hamburguer]"
    ) as HTMLButtonElement | null;
    const nav = document.querySelector("[data-nav]") as HTMLElement | null;

    if (hamburguer && nav && body) {
      hamburguer.addEventListener("click", () => {
        hamburguer.toggleAttribute("open");
        nav.toggleAttribute("open");
        body.classList.toggle("overflow-hidden");
      });
    }
  });
</script>
